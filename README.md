# java-maze-solver
This project is a Java implementation of a maze solver that uses recursive backtracking to count the number of valid paths from a start point to a finish point in a 2D grid. It demonstrates recursion, grid traversal, and search logic, along with basic test coverage for different maze scenarios.
